{
  "name": "ormb",
  "version": "0.0.0",
  "private": true,
  "description": "Testnet-first, whitelisted stablecoin issuance and enterprise settlement demo.",
  "type": "module",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "test": "node -e \"console.log('placeholder: tests not implemented yet')\"",
    "compile:contracts": "hardhat compile",
    "test:contracts": "hardhat test test/contracts/tooling.test.ts",
    "deploy:contracts": "node -e \"console.log('placeholder: contract deployment is not implemented; do not deploy from this branch')\"",
    "typecheck": "tsc --noEmit",
    "lint": "node -e \"console.log('placeholder: lint not implemented yet')\"",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate",
    "test:ci": "npm run lint && npm run prisma:generate && npm run typecheck && npm run prisma:validate && npm run test && npm run compile:contracts && npm run test:contracts && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@types/node": "^25.9.3",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "hardhat": "^3.9.0",
    "prisma": "^7.8.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.4.2",
    "next": "^16.2.9",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "viem": "^2.52.2"
  }
}
