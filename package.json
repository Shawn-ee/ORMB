{
  "name": "ormb",
  "version": "0.0.0",
  "private": true,
  "description": "Testnet-first, whitelisted stablecoin issuance and enterprise settlement demo.",
  "type": "module",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "node -e \"console.log('placeholder: dev server not implemented yet')\"",
    "build": "node -e \"console.log('placeholder: build not implemented yet')\"",
    "test": "node -e \"console.log('placeholder: tests not implemented yet')\"",
    "compile:contracts": "hardhat compile",
    "test:contracts": "hardhat test test/contracts/tooling.test.ts",
    "deploy:contracts": "node -e \"console.log('placeholder: contract deployment is not implemented; do not deploy from this branch')\"",
    "typecheck": "tsc --noEmit",
    "lint": "node -e \"console.log('placeholder: lint not implemented yet')\"",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate",
    "test:ci": "npm run lint && npm run typecheck && npm run prisma:validate && npm run test && npm run compile:contracts && npm run test:contracts && npm run build"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@types/node": "^25.9.3",
    "hardhat": "^3.9.0",
    "prisma": "^7.8.0",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "@prisma/client": "^7.8.0",
    "dotenv": "^17.4.2",
    "viem": "^2.52.2"
  }
}
